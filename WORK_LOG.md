# WORK_LOG — ドリームダック カラバリ仕様書

## 2026-07-02（2）— 3Dフォルム8点化 ＋ ギャラリー内インライン3Dプレビュー
- **新規OBJ 3点を受領→STL変換**：`口閉じ、羽横、顔通常`（真の基本形＝閉じ・横・普通、winkなし）／`口開き、羽前、顔通常`（開き・前）／`口開き、羽横、顔斜め`（**首傾き**・H58.1）。これで首傾き系・開き×前もカバー、計8フォルム。
- **ギャラリーの各カードにインライン3Dプレビューを実装**：Three.js、自動回転＋ドラッグ操作。ネットワーク集中を避け起動時に180ms間隔で段階読込。IntersectionObserverは画面外の描画停止（省電力）のみに使用。
- **拡大表示は ⛶ ボタンでモーダル起動**（従来の「▶3Dプレビュー」から役割変更）。
- 全8点に実測外寸（W×D×H mm）を表示。導入文を8点版へ更新。
- 公開反映：https://n-lyric.github.io/dreamduck-colorway/

## 2026-07-02 — 3Dデータ連携 ＋ GitHub Pages 公開
- **OBJ→STL変換**：Nomad Sculpt書出しの頂点カラーOBJ5点 → バイナリSTL（依存なし自作コンバータ・クアッド三角形化・法線自動計算）。STLは色非対応のためジオメトリのみ。
- **仕様書サイトに「3Dフォルムデータ」セクション追加**：5フォルムをカード表示（口ばし/羽/首バッジ）＋各OBJ/STL DLリンク＋Three.jsによるその場3Dプレビュー（STL読込・オービット操作）。
- **`プレビュー起動.command` 同梱**：file://直開きだと3Dが読めないため、localhost:8765を立てる起動スクリプト。
- **GitHub公開**：Lyric専用組織 `n-lyric` を新設 → リポ `n-lyric/dreamduck-colorway`（Public）作成・push・Pages有効化。
  - 公開URL：**https://n-lyric.github.io/dreamduck-colorway/**
  - 保護：noindex のみ（PWゲート無し）。未発表商品情報のため取扱注意。
  - リポ除外：`*.xlsx`（元データ58MB）・`images/_old_v1_20260526/`・`.DS_Store`・`.claude/`。
  - 注意：中断時の誤操作で個人側に空リポ `toshiokikuchi777-sudo/dreamduck-colorway` が残存。要削除（delete_repoスコープ or Web）。

## 2026-06-25 — 初回ラインナップ確定版へ更新
- ソース：`ドリームダック初回ラインナップ.xlsx`（リリック支給／6/25受領）を正とし、`index.html` を全面更新。
- 旧版（2026.05.26受領）からの変更点：
  - **並び替え**：旧ソフトホワイト起点 → 新パールホワイト①〜オーロライエロー⑬の連番。
  - **名称変更**：ソフトホワイト→パールホワイト、オーロラクリア→オーロライエロー。
  - **パーツ仕様を簡素化**：旧4パーツ（目/嘴/羽/毛）→ 新3パーツ（口ばし開閉／羽 前横／首 普通傾き）＝最大8フォルム。基本形は「閉じ・横・普通」。
  - **3Dレンダー画像を差し替え**：xlsx埋め込みのimage1〜13 → `images/duck_01〜13.png` に上書き。旧画像は `images/_old_v1_20260526/` にバックアップ。
  - 運勢13種・1行コピーは据え置き（変更なし）。
  - Pantone／核(⑨)・シークレット(⑬)・おみくじCTAは継続。
- 要確認（リリックへ）：
  1. ⑬オーロライエローのPantone未確定（旧版は⑨と012C重複表記）。
  2. ⑥ハートフェルトピンク と ⑦ムードグラデPINK の店頭識別性。
- 補足：パーツA/B対応＝口ばし(A:開き/B:閉じ)・羽(A:前/B:横)・首(A:普通/B:傾き)。特殊フォルム使用色＝口ばし開き③④⑧⑩／羽前⑥⑧⑪／首傾き④⑤。
